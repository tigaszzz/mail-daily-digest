# Snyk policy — mail-daily-digest
version: v1.25.0

# Snyk Code: excluir loopback OAuth (HTTP em 127.0.0.1 é obrigatório por RFC 8252 / Google & Microsoft desktop)
exclude:
  global:
    - src/services/oauth-loopback.js

ignore: {}

patch: {}
